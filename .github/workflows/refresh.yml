name: Refresh market data

on:
  workflow_dispatch:
  push:
    branches: [main]
    paths:
      - "relay.py"
      - "test_relay.py"
      - "universe_cli.py"
      - "config/universe.json"
      - "config/analysis.json"
      - ".github/workflows/refresh.yml"
  schedule:
    # 08:35 KST Mon-Fri = 23:35 UTC Sun-Thu
    - cron: "35 23 * * 0-4"

    # 09:35-20:35 KST Mon-Fri = 00:35-11:35 UTC Mon-Fri
    - cron: "35 0-11 * * 1-5"

permissions:
  contents: write

concurrency:
  group: market-data-write-${{ github.ref }}
  cancel-in-progress: false

jobs:
  refresh:
    runs-on: ubuntu-latest
    timeout-minutes: 15

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Check KRX trading day
        id: market_day
        shell: bash
        run: |
          set +e
          python krx_market_day.py
          code=$?
          if [ "$code" -eq 0 ]; then
            echo "run=true" >> "$GITHUB_OUTPUT"
          else
            echo "run=false" >> "$GITHUB_OUTPUT"
            echo "Skipping refresh (closed market or unverifiable KRX calendar)."
          fi
          exit 0

      - name: Run tests
        if: steps.market_day.outputs.run == 'true'
        run: python -m unittest discover -s . -v

      - name: Collect NAVER data
        id: collect
        if: steps.market_day.outputs.run == 'true'
        shell: bash
        run: |
          set +e
          python relay.py all
          code=$?
          echo "exit_code=$code" >> "$GITHUB_OUTPUT"
          exit 0

      - name: Commit refreshed data
        if: steps.market_day.outputs.run == 'true'
        shell: bash
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data

          if git diff --cached --quiet; then
            echo "No data changes to commit"
          else
            git commit -m "Refresh NAVER market data"
            git push
          fi

      - name: Fail after publishing error files
        if: steps.market_day.outputs.run == 'true' && steps.collect.outputs.exit_code != '0'
        run: |
          echo "Collector reported one or more failures."
          exit 1
